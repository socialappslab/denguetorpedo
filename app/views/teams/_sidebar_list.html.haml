- teams.shuffle.each do |team|
  .span5{:style => "margin-bottom: 3px;"}
    = link_to image_tag(team.picture, :style => "width: 50px; height: 50px;", :class => "img-rounded"), team_path(team)
    = link_to team.name, team_path(team)
