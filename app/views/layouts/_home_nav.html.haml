%nav.navbar.navbar-default#header
  .container-fluid
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle.collapsed{"aria-expanded" => "false", "data-target" => "#header-collapse", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to (@current_user.present? ? user_path(@current_user) : root_path), :class => "navbar-brand" do
        = logo_image

    #header-collapse.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li
          = link_to I18n.t("views.footer.about"), about_path
        -# %li.dropdown
        -#   %a.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :href => "#", :role => "button"}
        -#     = I18n.t("home_navigation.education.title")
        -#     %span.caret
        -#   %ul.dropdown-menu
        -#     - if I18n.locale.to_s == User::Locales::SPANISH
        -#       = render :partial => "home/education_dropdown_es"
        -#     - else
        -#       = render :partial => "home/education_dropdown_pt"
        -#
        -# %li.dropdown
        -#   %a.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :href => "#", :role => "button"}
        -#     = I18n.t("home_navigation.howto.title")
        -#     %span.caret
        -#   %ul.dropdown-menu
        -#     - DocumentationSection.order("order_id ASC").each do |section|
        -#       %li
        -#         = link_to section.title, howto_path + "##{section.html_id_tag}"
        -# %li
        -#   = link_to I18n.t("home_navigation.prizes.title") + " " + I18n.t("attributes.points"), prizes_path
      %ul.nav.navbar-nav.navbar-right
        = form_tag(session_path, :class => "navbar-form navbar-left form-inline") do
          .form-group
            = text_field_tag :username, nil, :placeholder => I18n.t("views.home.login.username"), :class => "form-control", :style => "min-width: 230px;"
            = password_field_tag :password, nil, :placeholder => I18n.t("activerecord.attributes.user.password"), :class => "form-control"
          = submit_tag "Entrar", :class => "btn btn-default"
        %li
          = link_to I18n.t("login_form.forgot_password"), new_password_reset_path
        %li
          - if I18n.locale.to_s == User::Locales::SPANISH
            = link_to "Português", "?locale=pt", :style => "display: inline-block; text-align: right;"
          - else
            = link_to "Español", "?locale=es", :style => "display: inline-block; text-align: right;"
