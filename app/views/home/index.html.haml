/------------------------------------------------------------------------------
= render :partial => "shared/flashes"
= render :partial => 'orange_slider'
%br

/------------------------------------------------------------------------------

/ We only want to display all neighborhood pictures to visitors. If a user
/ is logged in, they care only about their neighborhood.
- if @current_user.nil?
  .row
    .span10.offset1
      %h2
        = I18n.t("landing_page.community.title")
    .span5
      = form_tag neighborhood_search_path, :style => "margin: 0px; text-align: right;" do
        - searchable_neighborhoods = Neighborhood.all.map{|n| [n.name, neighborhood_path(n)] }
        - searchable_neighborhoods << [I18n.t("landing_page.community.future_community"), neighborhood_invitation_path]
        = select_tag("neighborhood[name]", options_for_select( searchable_neighborhoods  ), :prompt => I18n.t("landing_page.community.search_for_other_communities"), :id => "search-neighborhoods")

  .row
    .span16.offset1
      = render "home/neighborhood_list"

%br

/------------------------------------------------------------------------------

/ Display the neighborhood houses only if the logged-in user is viewing it.

- if @current_user.present?
  .row
  %h2.offset1#home_houses{style: 'font-size: 18px;'}
    = I18n.t("landing_page.house.title_with_name", :name => @selected_neighborhood.name)
- else
  .row
  %h2.offset1#home_houses{style: 'font-size: 18px;'}
    = I18n.t("landing_page.house.title")

.row
  #houses.span15.offset1
    = render "home/house_list"


/------------------------------------------------------------------------------

%br
.row
  %h2.offset1{style: 'font-size: 18px;'}
    = link_to I18n.t("activerecord.models.prize", :count => 3), prizes_path
  #sponsors.offset1{style: "margin-left:61px;"}
    %ul
      - @prizes.each do |prize|
        = render :partial => "prizes/prize", :object => prize

      %li.span3.prizes.possible
        .status.possible
          .name{style: "height:20px;"}
          .points
        .prize_content
          .description{:style => "height: 100%;"}
            %p{:style => "font-size: 30px; text-align: center; line-height: 75px;"}
              = link_to "+ #{I18n.t("common_terms.more")}", prizes_path


/------------------------------------------------------------------------------

/ Display neighborhood news only if a logged-in user is viewing it.
- if @current_user.present?
  .row
    .offset1
      #neighborhood_news
        %h2
          = I18n.t("landing_page.news.title")
        %ul
          - @notices.each do |notice|
            %a{:href => notice_path(notice)}
              %li.span4.neighborhood_news_title
                .row
                  .span5
                    - if notice.title.size > 28
                      = notice.title[0..25] + "..."
                    - else
                      = notice.title


/------------------------------------------------------------------------------

- if @current_user.blank?
  %br
  .row
    %h2.offset1{style: 'font-size: 18px;'}
      = I18n.t("common_terms.partners")
    #partners.offset1{:style => "font-weight:normal;line-height:25px;font-size:14px;"}
      <a href="http://www.gatesfoundation.org"> Bill & Melinda Gates Foundation</a> / <a href="http://citris-uc.org" >CITRIS </a> / <a href="http://www.research.ibm.com/labs/brazil/index.shtml">IBM Research Brazil</a> / <a href="http://www.inria.fr">Inria</a> / <a href="http://www.salud.carlosslim.org/">Instituto Carlos Slim de la Salud</a> / <a href="http://ipprio.rio.rj.gov.br/">Instituto Pereira Passos</a> / <a href="http://redesdamare.org.br/">Redes da Maré</a> / <a href="http://www.rio.rj.gov.br/web/sms">Secretaria Municipal de Saúde do Rio de Janeiro</a> / <a href="http://www.ubs.com/global/en/wealth_management/optimusfoundation.html">UBS Optimus Foundation</a> / <a href="http://www.berkeley.edu/index.html">UC Berkeley</a> / <a href="http://www.ufrj.br">UFRJ</a>

/------------------------------------------------------------------------------

:javascript
  $(document).ready(function()
  {
    $("#search-neighborhoods").change(function() {
      window.location.href = $(this).val()
    });
  })
