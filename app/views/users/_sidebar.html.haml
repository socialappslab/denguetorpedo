
%div{:style => "margin-left: 30px; margin-right: 30px;"}
  .user_profile_picture{:style => "text-align: center;"}
    - if @user.profile_photo_file_name.nil?
      = image_tag("default_images/profile_default_image.png", :style => " width: 100px;")
    - else
      = image_tag(@user.profile_photo.url(:large), :style => " width: 100px;")

    %div{:style => "text-align: center;"}
      = @user.first_name
    -# - if @user.role == "lojista"
    -#   #{@user.shorter_display_name} (patrocinador)
    -# - elsif @user.role == "verificador"
    -#   - if @user.gender == true
    -#     #{@user.shorter_display_name} (verificador)
    -#   - else
    -#     #{@user.shorter_display_name} (verificadora)
    -# -elsif @user.role == "coordenador"
    -#   - if @user.gender == true
    -#     #{@user.shorter_display_name} (coordenador)
    -#   - else
    -#     #{@user.shorter_display_name} (coordenadora)
    -# - else
    -#   = @user.shorter_display_name



  .row
    - if @user == @current_user
      = link_to edit_user_path(@current_user), :style => "display: block; font-size: 15px; margin-top: 20px;" do
        %i.fa.fa-gear.fa-2
        = I18n.t("common_terms.configuration")

    - if @user == @current_user
      - house_link_text = I18n.t("dashboard.my_house")
      - community_link_text = I18n.t("dashboard.my_community")
    - else
      - house_link_text = I18n.t("common_terms.their_house")
      - community_link_text = I18n.t("common_terms.their_community")

    - if @user.neighborhood && @user.house
      = link_to neighborhood_house_path(@user.neighborhood, @user.house), :style => "display: block; font-size: 15px; margin-top: 20px;" do
        %i.fa.fa-home.fa-2
        = house_link_text

    - if @user.neighborhood
      = link_to neighborhood_path(@user.neighborhood), :style => "display: block; font-size: 15px; margin-top: 20px;" do
        %i.fa.fa-users.fa-2
        = community_link_text

  %div{:style => "margin-top: 20px;"}

  = render :partial => "users/badges"
