.row.feed-item-bar
  .span7
    = link_to user.display_name, user_path(user), :style => "font-weight: bold;"
    .feed-item-timestamp
      %i.fa.fa-clock-o
      = format_timestamp(item.created_at)

    - if @current_user && @current_user == user && item.class == Post
      = link_to path, :id => 'user_post_delete', :confirm => I18n.t("views.alerts.confirm"), :method => :delete do
        %i.fa.fa-trash-o
