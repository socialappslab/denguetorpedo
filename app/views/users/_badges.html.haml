.row{:style => "text-align: center;"}
  - if @user.total_points >= 100
    = image_tag("badges/de_olho.png", :style => "width: 75px; margin-top: 10px;")
  - if @user.total_points >= 450
    = image_tag("badges/exterminador.png", :style => "width: 75px; margin-top: 10px;")
  - if @user.total_points >= 900
    = image_tag("badges/guerreiro.png", :style => "width: 75px; margin-top: 10px;")
  - if @user.total_points >= 1350
    = image_tag("badges/saudavel.png", :style => "width: 75px; margin-top: 10px;")
  - if @user.total_points >= 1800
    = image_tag("badges/cuido.png", :style => "width: 75px; margin-top: 10px;")

  -# %ul#coupons_list{style: "margin-left:10px;"}
  -#   - @coupons.reverse.each do |coupon|
  -#     = render :partial => "prize_codes/coupon", :object => coupon
