#newsfeed
  - for item in @activity_feed
    - if item.is_a? Post
      = render :partial => 'users/feed_item', :locals => {:post => item, :owner => item.user}
    - elsif item.is_a? Notice
      = render :partial => 'notices/feed_item', :locals => {:notice => item, :owner => item.user}
